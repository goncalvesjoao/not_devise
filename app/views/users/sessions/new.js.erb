(function () {
  var oldNewFormContainer = document.getElementById("new-form-container");
  var newFormContainer = document.createElement('div');

  newFormContainer.innerHTML = "<%= j render(template: 'users/sessions/new', formats: [:html]) %>";

  oldNewFormContainer.replaceWith(newFormContainer.firstChild);
})()
